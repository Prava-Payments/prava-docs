---
title: 'Changelog'
description: "What's new in Prava and these docs."
icon: 'clock-rotate-left'
rss: true
---

<Update
  label="September 8, 2026"
  tags={["Improvements", "Fixes", "API"]}
  rss={{
    title: "Mexico-issued cards and enrollment retry improvements",
    description: "Production card eligibility now includes Mexico, subject to existing issuer restrictions. Full card enrollment can retry Visa initialization failures before a card has been provisioned."
  }}
>
  ## Mexico-issued cards and enrollment retry improvements

  - **Card eligibility:** Mexico-issued cards now pass Prava's issuing-country check. Existing
    issuer restrictions still apply.
  - **Enrollment retries:** If Visa verification fails to initialize before a card is provisioned,
    **Try again** now starts a fresh readiness cycle in the same enrollment session.
  - **Card artwork:** Full-enrollment completion can display the card artwork when it is available.

  These improvements are available in production.

  [Integrate card enrollment →](/api-reference/enroll-card)
</Update>

<Update
  label="September 2026 — Sessions"
  description="Live API roundup"
  tags={["New", "Breaking Changes", "API"]}
  rss={{
    title: "Create payment sessions from an existing quote",
    description: "The live session API accepts quote-based purchase context. Custom integrations must wrap their former purchase-context array in a custom object. Quote amounts and currencies must match; invalid merchant URLs and email domains are rejected at creation."
  }}
>
  ## Create payment sessions from an existing quote

  `POST /v1/sessions` supports a quote-based flow: pass the quote ID and Prava resolves
  its merchant, products, and final total before creating the payment session.
  From your backend, authenticate with your merchant secret key and send:

  ```json
  {
    "user_id": "customer_123",
    "user_email": "customer@example.com",
    "total_amount": "49.99",
    "currency": "USD",
    "purchase_context": {
      "quote": true,
      "quote_id": "qte_..."
    }
  }
  ```

  Use the exact final amount and currency of your quote. A mismatch returns
  `409 QUOTE_SESSION_MISMATCH`; an expired quote returns `410 QUOTE_EXPIRED`.
  Omit `custom` in quote mode.

  **Action required for custom checkout integrations:** Change
  `purchase_context: [...]` to `purchase_context: { custom: [...] }` on the same endpoint.
  Keep your existing merchant and product fields inside that array; one merchant is supported
  per session. Omit `quote_id` in custom mode.

  Session creation also validates merchant URLs and customer email domains earlier.
  Supply an HTTPS merchant URL with a valid public domain and a customer email on a
  routable domain. Merchant URLs are stored as their origin, with paths, queries, and fragments
  removed; merchant names are normalized to Visa-supported characters.

  [Create a session →](/api-reference/create-session)
</Update>

<Update
  label="September 2026 — Mandates"
  description="Live API roundup"
  tags={["New", "Breaking Changes", "API"]}
  rss={{
    title: "Manage standing mandates and reconcile each charge",
    description: "Set up mandates through sessions, filter by customer, inspect charge history, and pause, resume, or cancel future charging. Direct agent integrations must decrypt encrypted_payload when charging; merchant secret-key callers still receive credentials."
  }}
>
  ## Manage standing mandates and reconcile each charge

  Add `mandate_setup` with `intent: "mandate_setup"` to a session to collect approval
  for future charges. After activation, charge the mandate without another passkey prompt.
  One-time standing mandates are capped at seven days; a later `valid_until` does not extend
  that window. Check `validUntil` before scheduling a charge.

  - **Find the right mandate:** `GET /v1/mandates` accepts `customer_id` for merchant callers,
    using either their external customer ID or a Prava customer ID. Add `standing_only=true`
    to exclude transient checkout mandates. Responses include `approvedAmount`, `remaining`,
    `validUntil`, `renewsAt`, and `lastCharge`; the detail endpoint includes charge history.
    Displayed balances are derived from recorded activity; Visa enforces the spending limit.
  - **Track each charge:** `POST /v1/mandates/{id}/charge` creates its own order and transaction.
    Send a decimal-string `amount` and an optional `reference` to deduplicate retries within
    that mandate. Failed charges release the reference for retry. Currency comes from the
    mandate; an optional per-charge `purchase_context` remains a one-entry array.
  - **Report the outcome:** Send `txn_status: "APPROVED"` or `"DECLINED"` to
    `POST /v1/mandates/{id}/charges/{txnId}/report` to reconcile the charge and report to Visa.
    `txn_type` defaults to `"PURCHASE"`.
  - **Control future charging:** `POST /v1/mandates/{id}/pause`, `/resume`, and `/cancel`
    manage the mandate's Prava state. Cancellation is terminal in Prava; these controls
    do not revoke the underlying Visa instruction.

  **Action required for direct agent charge integrations:** Read and decrypt
  `encrypted_payload` using the agent's private key instead of reading plaintext
  `credentials` or `dynamicData`. Merchant secret-key callers still receive `credentials`.
  Charging and reporting require a merchant secret key or a direct agent signature;
  charging is not exposed through MCP.

  [Set up a mandate →](/api-reference/create-session#mandate-setup) ·
  [Charge →](/api-reference/mandate-charge) ·
  [Report →](/api-reference/mandate-report) ·
  [Lifecycle controls →](/api-reference/mandate-lifecycle)
</Update>

<Update
  label="September 2026 — Saved cards"
  description="Live API roundup"
  tags={["New", "Improvements", "API"]}
  rss={{
    title: "Saved-card defaults, removal, and passkey reset",
    description: "Saved-card lists identify the default card. Merchant integrations can change the default, cardholders can reset device bindings while keeping their card, and card removal reports whether its network token was retired."
  }}
>
  ## Saved-card defaults, removal, and passkey reset

  - **Default cards:** `GET /v1/listCards` returns `is_default` and places the default first.
    Merchant secret-key callers can set it with `PATCH /v1/cards/{id}/default` or clear it
    with `PATCH /v1/cards/remove-default`, passing the customer's Prava ID in the
    `customer_id` query parameter. When a card list has no default, the earliest saved card
    becomes the fallback, including after an explicit clear.
  - **Reset passkeys:** Cardholder session authentication can call
    `DELETE /v1/cards/{id}/device-bindings` to remove device bindings while keeping the
    saved card and network token. Local passkey state is cleared only after removal is
    verified. Shared-token bindings cannot be reset through this endpoint.
  - **Remove and re-save cards:** `POST /v1/deleteCard` accepts `CUSTOMER_CONFIRMED` for
    ordinary customer-requested removal and returns `was_default` and
    `network_token_deleted`. Shared network tokens are retained for other customers.
    Re-saving a removed card refreshes reused enrollment and token records for the new saved card.
  - **Issuing-country coverage:** Canada, Australia, Hong Kong, Japan, Malaysia,
    New Zealand, Singapore, South Korea, Taiwan, Thailand, and Vietnam pass the
    issuing-country check alongside the US and its supported territories. Existing
    issuer restrictions still apply; Mexico is covered in the September 8 update.

  [List cards →](/api-reference/list-cards) ·
  [Delete a card →](/api-reference/delete-card)
</Update>

<Update
  label="September 2026 — Agent sessions"
  description="Live API roundup"
  tags={["Improvements", "API", "CLI"]}
  rss={{
    title: "Replay agent-session requests and check client compatibility",
    description: "Agent-session creation accepts an Idempotency-Key and returns the original session on replay. API response headers advertise CLI and per-skill compatibility requirements."
  }}
>
  ## Replay agent-session requests and check client compatibility

  Direct signed-agent integrations can send an `Idempotency-Key` with
  `POST /v1/sessions/agent`. Reusing it for the same agent returns the original session
  with HTTP `200`; a new session returns `201`. A concurrent duplicate can return
  `409 DUPLICATE_EXTERNAL_ORDER_REF`; retry with the same key to retrieve the original.

  This endpoint accepts flat `total_amount`, `currency`, `merchant_name`, `merchant_url`,
  `merchant_country`, and `products` fields. Read the approval link from `payment_url`.
  Use a new idempotency key for each new purchase.

  API responses also expose `X-Min-CLI-Version` and `X-Min-Skill-Version`.
  Set `X-Skill-Name` to receive the requirement for that skill. The current production
  CLI minimum is `3.2.0`.

  [Use the current CLI →](/prava-pay/quickstart)
</Update>

<Update
  label="September 2026 — Checkout"
  description="Live feature roundup"
  tags={["Improvements", "Fixes"]}
  rss={{
    title: "Branded embedded checkout and clearer payment recovery",
    description: "Embedded checkout follows merchant branding. Mobile card and verification screens fit more reliably, retries refresh saved cards, and payment authorization uses the full approved total including tax and shipping."
  }}
>
  ## Branded embedded checkout and clearer payment recovery

  Set `integration_type: "embedding"` when creating a session for a full-width checkout
  that uses your stored merchant brand color. The default `"full_checkout"` mode provides
  the standalone checkout presentation.

  - Mobile layouts keep expiry fields, card controls, verification steps, and the footer
    visible, including on iPhones with a bottom safe area.
  - Retrying checkout refreshes saved cards, restores add-card controls, and clears stale
    error overlays so newly added cards are available.
  - Visa verification initialization has timeout and retry handling. Card-vault connection
    failures have bounded retries before provisioning, and card-art generation has limits
    to reduce interference with payment requests.
  - Payment authorization uses the full approved order total, including tax and shipping.
    Currency handling uses the order's supported currency throughout the payment flow.

  [Choose a checkout presentation →](/api-reference/create-session)
</Update>

<Update
  label="September 2026 — Developer dashboard"
  description="Live sandbox-hosted dashboard roundup"
  tags={["New", "Improvements"]}
  rss={{
    title: "Named API keys, staged rotation, and clearer order history",
    description: "The sandbox-hosted developer dashboard supports named keys, staged rotation, business-profile and checkout-branding edits, and clearer order history. Production views require production access."
  }}
>
  ## Named API keys, staged rotation, and clearer order history

  The sandbox-hosted developer dashboard now lets you name API keys and see their creation
  date, last use, and status. New secret keys are shown once. Rotate a key by creating and
  saving its replacement, updating your integration, and then explicitly revoking the old key.
  The old key stays active until you revoke it.

  Key creation requires the developer role or higher; revocation requires admin access.
  Newly issued keys use `pk_test` / `sk_test` prefixes for sandbox and `pk_live` / `sk_live`
  for production. Production views and actions require production access.

  Order details also show recorded card-check outcomes per payment attempt and a readable
  activity timeline, making declined and retried attempts easier to follow.

  After setting up a merchant in the selected environment, use **Business Details** to
  update the application name, website, and embedded-checkout brand color. Production
  access remains subject to approval.

  [Manage your integration in the dashboard →](/dashboard)
</Update>

<Update
  label="September 2026 — Reseller workspaces"
  description="Live sandbox-hosted dashboard roundup"
  tags={["New", "Improvements"]}
  rss={{
    title: "Manage AI applications in enabled reseller workspaces",
    description: "Enabled reseller workspaces can track application onboarding, review payment activity, and rotate credentials when the relevant modules are enabled. The Team view lists active members and pending invitations."
  }}
>
  ## Manage AI applications in enabled reseller workspaces

  Enabled reseller workspaces in the sandbox-hosted dashboard have dedicated Overview,
  Applications, and Team views. With merchant onboarding enabled, submit an application,
  follow its review status, and respond to requested changes before activation.

  Authorized workspaces can inspect their applications' orders and transactions and,
  with credential management enabled, create a replacement key before revoking the old one.
  The read-only Team view lists active members and pending invitations.

  Availability follows the reseller's enabled environment and modules. Each order retains
  its own currency; application analytics avoid combining amounts across currencies.

  [Open the dashboard →](/dashboard)
</Update>

<Update
  label="September 2026"
  tags={["Improvements", "Breaking Changes", "API"]}
  rss={{
    title: "Payment-result card identity and quote context",
    description: "Payment results identify the transaction card. Quote integrations must read merchant_res instead of shop_pay and keep polling while the result is processing."
  }}
>
  ## Payment-result card identity and quote context

  - `GET /v1/sessions/{sessionId}/payment-result` now returns `transactions[].card_id`, the source
    of truth for the card bound to the transaction whether preselected or chosen during collection.
  - Quote-backed results return credential-free transaction summaries and expose merchant checkout
    lifecycle context as `merchant_res`. Non-quote and no-order responses omit the lifecycle field.
  - Results can report `status: "processing"` while payment or merchant checkout is still
    running. For quote-backed sessions, keep polling for the merchant's terminal outcome;
    credential issuance alone does not mean the merchant order is paid.

  **Action required:** Quote integrations that previously read `shop_pay` must now read
  `merchant_res`. Treat `processing` as nonterminal and keep polling.
</Update>

<Update
  label="September 6, 2026"
  tags={["New", "API"]}
  rss={{
    title: "Enroll a card and set up its passkey without a payment",
    description: "Merchant full-enrollment sessions collect or reuse a vaulted card and complete passkey setup without creating an order or transaction. Create the session with your secret key, open its hosted URL, and poll for completion."
  }}
>
  ## Enroll a card and set up its passkey without a payment

  Prepare a customer's card for future payments in a dedicated enrollment flow.
  `POST /v1/sessions/enroll-card` now accepts `enrollment_mode: "full_enrollment"`
  and returns a session that the customer completes on Prava's secure surface.

  Start with an existing `vault_ref_id`, or omit it to let the customer enter a card.
  Full enrollment provisions the card and completes its Visa enrollment and passkey setup
  without creating an order or transaction. The existing `provision_only` mode continues
  to return a `card_id` directly for an already-vaulted card.

  Create the session on your backend using your secret API key:

  ```json
  {
    "enrollment_mode": "full_enrollment",
    "user_id": "customer_123",
    "user_email": "customer@example.com",
    "integration_type": "full_checkout"
  }
  ```

  Open the returned `iframe_url` unchanged. Your backend can poll
  `GET /v1/sessions/{sessionId}/status` to distinguish a provisioned card from completed
  enrollment. Accept success when `status` is `completed` and `outcome` is `fully_enrolled`.
  Retryable failures can be retried inside the same session using the selected card.

  [Set up card enrollment →](/api-reference/enroll-card) ·
  [Poll enrollment status →](/api-reference/get-card-enrollment-status)
</Update>

<Update
  label="September 1, 2026"
  description="Dashboard sandbox deployment"
  tags={["Improvements"]}
  rss={{
    title: "Clearer dashboard environment switching",
    description: "The sandbox-hosted developer dashboard coordinates environment changes across tabs, shows recoverable switch failures, and checks for unsaved changes before switching."
  }}
>
  ## Clearer dashboard environment switching

  The developer dashboard now coordinates environment changes across open tabs and shows
  a transition while the selected view refreshes. Failed switches offer a recovery action.
  Switching checks for unsaved edits and one-time API keys, and entering the production
  view asks for confirmation.

  This update is deployed to the sandbox-hosted dashboard. The production view is available
  to accounts with production access.

  [Using dashboard environments →](/dashboard)
</Update>

<Update
  label="August 28, 2026"
  tags={["New", "API"]}
  rss={{
    title: "Provision a vaulted card from your backend",
    description: "Merchant secret keys can call the card-enrollment API to provision an already-vaulted customer card and receive its card_id without creating a payment."
  }}
>
  ## Provision a vaulted card from your backend

  Merchant integrations can now call `POST /v1/sessions/enroll-card` with a secret API key
  to provision an already-vaulted customer card. Supply the customer identity, `vault_ref_id`,
  and consent reference; the response returns the resulting `card_id`.

  This server-to-server flow creates no order or transaction. It provisions the card;
  passkey setup and payment authorization are separate steps.

  [Provision a card →](/api-reference/enroll-card)
</Update>

<Update
  label="August 11, 2026"
  description="@prava-sdk/cli 3.2.0"
  tags={["Fixes", "CLI"]}
  rss={{
    title: "CLI mandate setup uses the agent session API",
    description: "Prava CLI 3.2.0 fixes mandate creation by using the agent session endpoint and reading its payment URL. Users of CLI 3.1.0 should update before creating a mandate."
  }}
>
  ## CLI mandate setup uses the agent session API

  `@prava-sdk/cli` 3.2.0 fixes `prava mandate create`: it now sends the agent-session
  request format and displays the returned approval URL. Version 3.1.0 sent the request
  to the merchant-session endpoint and expected a different response field.

  **Action required:** If you use 3.1.0 to create mandates, update to 3.2.0 or later.

  [Create a mandate with the CLI →](/prava-pay/mandates)
</Update>

<Update
  label="July 2026"
  tags={["New", "MCP", "Docs"]}
  rss={{
    title: "Prava MCP and expanded developer documentation",
    description: "MCP connection instructions, end-to-end guides, API references, agent skills, and AI-assisted documentation access."
  }}
>
  ## Prava MCP is live

  - Connect any MCP client to `https://mcp.pay.prava.space/mcp` and your agent can pay and shop
    with your card. One URL, OAuth sign-in, 12 tools, and a credential firewall: payment
    credentials never reach the agent. Start at [Prava MCP](/mcp/overview).

  ### Docs

  - New end-to-end guides: [Add Payments to Your AI App](/guides/add-payments-to-your-ai-app),
    [REST Checkout Walkthrough](/guides/rest-checkout-walkthrough), and the
    [Go-Live Checklist](/guides/go-live-checklist).
  - New reference pages: [Errors](/api-reference/errors) (every code in one catalog),
    [Testing in Sandbox](/api-reference/testing), and [Delete Card](/api-reference/delete-card).
  - New audience pages: [Your Prava Pay Dashboard](/prava-pay/your-wallet) for agent owners,
    [For Merchants](/integration/merchants), [Use Cases](/use-cases), and
    [Compliance & Verification](/guides/compliance).
  - Sandbox [test cards and test OTP](/api-reference/test-cards) published.
  - Navigation reorganized: SDK, MCP, and CLI are sibling integration methods under Developer
    Guide; Capabilities and FAQ have their own tabs.
  - [Payments](/concepts/payments) restructured around the public flow — what you drive vs. what
    Prava does for you — plus a new [Glossary](/concepts/glossary).
  - Fresh visual identity: brand fonts, light theme, imagery, and an interactive API playground
    backed by an OpenAPI spec.
  - Agent skills: `prava-pay`, `prava-shopping`, and `prava-sdk-integration` installable via
    `npx skills add`.
  - Docs MCP server at `docs.prava.space/mcp` and `llms.txt` for AI-assisted integration —
    see [Use Prava Docs with AI](/ai).

  ### API

  - `POST /v1/deleteCard` documented in the API reference and OpenAPI spec.
</Update>

<Update
  label="July 2, 2026"
  description="@prava-sdk/cli 3.0.0"
  tags={["New", "CLI"]}
  rss={{
    title: "Shopping commands in Prava CLI v3",
    description: "Prava CLI 3.0.0 was published on July 2 with product search, product details, quote, checkout, and delivery-address commands."
  }}
>
  <span id="june-2026" />

  ## Shopping commands in Prava CLI v3

  `@prava-sdk/cli` 3.0.0 adds `prava shop search`, `product`, `quote`, and `checkout`
  commands alongside the existing agent-linking and payment-session commands.
  The package also includes commands to list, add, and choose a default delivery address.

  [Explore CLI shopping commands →](/prava-pay/shopping)
</Update>

<Update
  label="May 30, 2026"
  description="@prava-sdk/cli 2.0.0"
  tags={["Improvements", "CLI"]}
  rss={{
    title: "Shorter agent approval links and clearer setup recovery",
    description: "Prava CLI 2.0.0 signs agent registration requests, uses short server-issued approval links, and explains expired links and incorrect system clocks during setup."
  }}
>
  ## Shorter agent approval links and clearer setup recovery

  `@prava-sdk/cli` 2.0.0 updates `prava setup` to register a signed request and print
  a short approval link containing a server-issued link ID. Agent metadata is submitted
  during registration instead of being carried in the approval URL.

  Setup reports expired or denied links and explains when an incorrect system clock
  prevents registration. Polling stops when a link expires and tells you to start setup again.

  [Link an agent →](/prava-pay/linking)
</Update>

<Update
  label="May 16, 2026"
  description="Dashboard sandbox deployment"
  tags={["Improvements"]}
  rss={{
    title: "More visible API-key creation errors",
    description: "The sandbox-hosted dashboard displays API-key creation failures as notifications in the key-creation flow."
  }}
>
  ## More visible API-key creation errors

  API-key creation failures now appear as notifications in the sandbox-hosted dashboard,
  making unsuccessful requests easier to spot while the key-creation dialog remains open.

  [Manage API keys →](/dashboard)
</Update>

<Update
  label="May 13, 2026"
  description="Production enrollment milestone"
  tags={["New", "Fixes", "API"]}
  rss={{
    title: "Live card enrollment verified in production",
    description: "Prava verified its first live cardholder enrollment against production Visa services. Production API connectivity and Visa verification configuration fixes supported this milestone."
  }}
>
  ## Live card enrollment verified in production

  Prava's first live cardholder enrollment was verified against production Visa services.
  The verified flow covers session creation, hosted card verification, and enrollment
  for the initial production integration.

  Related fixes restored production API requests that had blocked merchant onboarding
  and corrected the Visa environment used by hosted verification.

  [Understand the checkout flow →](/concepts/checkout-flow)
</Update>

<Update
  label="May 5, 2026"
  description="@prava-sdk/cli 0.0.1"
  tags={["New", "CLI"]}
  rss={{
    title: "The first Prava CLI package",
    description: "Prava CLI 0.0.1 was published with commands for agent linking, link-status checks, and creating payment sessions with polling."
  }}
>
  ## The first Prava CLI package

  The first public `@prava-sdk/cli` package introduces the `prava` command for agent
  integrations. Version 0.0.1 includes `prava setup`, `prava status`, and
  `prava sessions create`, with polling for the payment-session result.

  [Get started with the current CLI →](/prava-pay/quickstart)
</Update>
